# requirements-minimal.txt
# ==============================================================================
# 智能设备预测性维护 — 最小安装（仅统计基线分析 + 决策引擎）
# Python 要求: 3.10+
#
# 安装方式:
#   pip install -r requirements-minimal.txt
#
# 此文件省略 torch、xgboost（ML推理）和 fastapi/uvicorn（Web服务）。
# 仅提供 数据预处理 → 统计推理 → 决策工单 的完整降级路径。
# 预计安装时间: <2分钟，磁盘占用: ~200MB。
#
# 验证命令:
#   python agent-mcp架构/agent_orchestrator.py --data-dir 原始数据集 --skip-ml --skip-diagnosis
# ==============================================================================

numpy>=1.24.0,<3.0
pandas>=2.0.0,<3.0
scipy>=1.9.0,<2.0
scikit-learn>=1.3.0,<2.0
matplotlib>=3.7.0
jinja2>=3.1.0
mcp>=1.20,<2.0
