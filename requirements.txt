# requirements.txt
# ==============================================================================
# 智能设备预测性维护 — 完整部署环境依赖
# Python 要求: 3.10+ (推荐 3.11/3.12)
#
# 安装方式:
#   pip install -r requirements.txt
#
# 如果只需要统计基线分析（跳过ML训练），请使用 requirements-minimal.txt
# ==============================================================================

# ── 核心科学计算 ──
numpy>=1.24.0,<3.0
pandas>=2.0.0,<3.0
scipy>=1.9.0,<2.0
scikit-learn>=1.3.0,<2.0

# ── 机器学习 ──
xgboost>=2.1.0
torch>=2.0.0
shap>=0.45.0

# ── 可视化 ──
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Web 服务 + Dashboard ──
fastapi>=0.100.0
uvicorn>=0.20.0
httpx>=0.24.0
jinja2>=3.1.0

# ── MCP 协议 (Agent 模式运行时) ──
mcp>=1.20,<2.0

# ── 报告生成 ──
python-docx>=1.0.0

# ── 可选: .env 配置 (web-dashboard 使用，缺失时自动降级) ──
python-dotenv>=1.0.0

# ── RAG 智能客服 (文档问答) ──
chromadb>=0.4.0
sentence-transformers>=3.0.0
